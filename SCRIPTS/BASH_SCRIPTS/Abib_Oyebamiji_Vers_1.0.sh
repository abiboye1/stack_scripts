#!/bin/bash

# Listing the content of the backup directory
ls -ltr backup

#Copying the zerobyte file into the backup directory
cp /home/oracle/scripts/practicedir_abi_sep23/zerobyte.txt /home/oracle/scripts/practicedir_abi_sep23/backup

#Listing the content of the backup directory
ls -ltr backup
